sudo: false

language: python

python:
  - "3.4"
  
addons:
  apt:
    packages:
    - python3
    - gir1.2-gtk-3.0
    - python3-gi-cairo
    - python3-mpltoolkits.basemap
    - python3-numpy
    - python3-matplotlib
    - python3-libhamlib2
    - python3-sphinx

before_install:
 - "export DISPLAY=:99.0"
 - "sh -e /etc/init.d/xvfb start"

install:
  - make install

before_script:
  - export PYTHONPATH=`pwd`:$PYTHONPATH
  
script:
  - make unittest
  - make docs
  - make clean
